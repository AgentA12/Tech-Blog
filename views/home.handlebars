<ul>
  {{#each this}}
    <li>
      {{!-- {{post.id}} --}}
      <div class="post-container">
        <div class="post-title">
          <h3></h3>
          <p>Posted by {{this.username}} on {{this.createdAt}}</p>
        </div>
        <div class="post-body">
          {{!-- <p>{{post.posts}}</p> --}}
        </div>
      </div>
    </li>
  {{/each}}
</ul>